# ADR-004 Mobile owns internal tooling

## Decision

All operator control (build, start, end, results) lives in the mobile app. Public web is respondent-first.

## Why

Session poll ranked mobile + internal tooling; combining them keeps the story clear: phone = desk, browser = audience.

## Consequences

A future admin web console is out of scope for v1 unless a new requirement is approved.
