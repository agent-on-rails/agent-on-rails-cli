# Local SQLite lives here (gitignored).

Default path: `./data/survey-desk.sqlite` (`SURVEY_DESK_DATABASE_PATH`).

Delete `*.sqlite*` before a clean demo so operators/emulators do not see prior sessions.
